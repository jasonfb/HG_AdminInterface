<div class="row">
  <div class='scaffold-cell form-group <%='alert-danger' if @person.errors.details.keys.include?(:name)%>' > 
  <%= f.text_field :name, value: @person.name, size: 40, class: 'form-control', type: '' %>
   <label class='form-text' >Name</label>
</div>

  <div class="">
    <%= link_to "Cancel", admin_account_persons_path,  {class: "btn btn-secondary"} %>
    <%= f.submit "Save", class: "btn btn-primary pull-right" %>
  </div>
</div>
